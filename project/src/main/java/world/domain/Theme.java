package world.domain;

/**
 * An enum of themes that the game supports. For each world, players can some themes to have additional content.
 */
public enum Theme {
    ;

    public static Theme fromName(String argument) {
        return null;
    }
}
